# Starship Configuration - Nord Minimal
# Subtle Nord colors, text-based, flat

format = """
$directory$git_branch$git_status$nodejs$rust$python$golang$cmd_duration
$character"""

[character]
success_symbol = "[✔](bold #a3be8c)" # Green check
error_symbol = "[✖](bold #bf616a)"   # Red cross

[directory]
style = "bold #88c0d0" # Nord Cyan
format = "[$path]($style) "
truncation_length = 3
truncate_to_repo = true
truncation_symbol = "…/"
fish_style_pwd_dir_length = 1
read_only = " 󰌾"

[git_branch]
symbol = " " # GitHub Icon
style = "bold #eceff4" # White
format = "[$symbol $branch]($style) "

[git_status]
style = "bold #ebcb8b"
format = "([$all_status$ahead_behind]($style) )"
conflicted = "⚠️"
ahead = "⬆️${count}"
behind = "⬇️${count}"
diverged = "🔃"
untracked = "❓"
stashed = "📦"
modified = "📝"
staged = "✅"
renamed = "🚚"
deleted = "🗑️"

[nodejs]
symbol = "󰎙 "
style = "bold #a3be8c"
format = "via [$symbol$version]($style) "

[cmd_duration]
min_time = 2_000
style = "bold #4c566a"
format = "($duration) "
